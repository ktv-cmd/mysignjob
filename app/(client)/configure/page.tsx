import SignConfigurator from "@/components/sign-configurator/SignConfigurator";

export const metadata = { title: "Build Your Sign — My Sign Job" };

export default function ConfigurePage() {
  return <SignConfigurator />;
}
